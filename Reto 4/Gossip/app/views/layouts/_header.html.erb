<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/">Gossip Chat</a>
      <div class="nav-collapse">
        <ul class="nav">
          <li><%= link_to "Rooms", rooms_path %></li>
          <% if current_user && current_user.admin %>
             <li><%= link_to "Users", users_path %></li>
          <% end %>
        </ul>
        <ul class="nav pull-right">
          <% if current_user %>
            <li><a class="brand" style="font-size:14px;" href="/users/<%= current_user.id%>"><%= current_user.username%></a></li>
            <li><%= link_to "Logout", signout_path, method: "delete" %></li>
          <% else %>
            <li><%= link_to "Login", signin_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>